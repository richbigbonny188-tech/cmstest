/**
 * This module is responsible to create a bootstrap assets bundle.
 *
 * Instead of including jquery and popper.js, we use bootstrap native
 * to execute js functionality for the bootstrap framework.
 */

import './styles.scss';
import 'bootstrap/dist/js/bootstrap.esm.min';
